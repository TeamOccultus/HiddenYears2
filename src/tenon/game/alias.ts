export class ItemAlias {
  constructor(
    public name: string,
    public alias: string
  ) {}
}

export class BlockAlias {
    constructor(
      public name: string,
      public alias: string
    ) {}
  }
  
