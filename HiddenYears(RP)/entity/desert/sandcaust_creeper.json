{
  "format_version": "1.8.0",
  "minecraft:client_entity": {
    "description": {
      "identifier": "hiddenyears:sandcaust_creeper",
      "materials": {
        "default": "creeper"
      },
      "textures": {
        "default": "textures/entity/sandcaust_creeper"
      },
      "geometry": {
        "default": "geometry.sandcaust_creeper"
      },
      "spawn_egg": {
        "texture": "hiddenyears:egg_sand_creeper"
      },
      "scripts": {
        "pre_animation": [
          "variable.wobble = Math.sin(query.swell_amount * 5730) * query.swell_amount * 0.01 + 1.0;",
          "variable.swelling_scale1 = (Math.pow(Math.clamp(query.swell_amount, 0.0, 1.0), 4.0) * 0.4 + 1.0) * variable.wobble;",
          "variable.swelling_scale2 = (Math.pow(Math.clamp(query.swell_amount, 0.0, 1.0), 4.0) * 0.1 + 1.0) / variable.wobble;",
          "variable.leg_rot = Math.cos(query.modified_distance_moved * 38.17326) * 80.22 * query.modified_move_speed;",
          "variable.flash = Math.mod(Math.Round(query.swell_amount * 10.0), 2.0);"
        ]
      },
      "animations": {
        "walk": "animation.sandcaust_creeper.walk",
        "idle": "animation.sandcaust_creeper.idle"
      },
      "animation_controllers": [
        {
          "sandcaust_creeper_walk": "controller.animation.sandcaust_creeper.walk"
        }
      ],
      "render_controllers": ["controller.render.sandcaust_creeper"]
    }
  }
}
